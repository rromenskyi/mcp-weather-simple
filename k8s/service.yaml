apiVersion: v1
kind: Service
metadata:
  name: mcp-weather
  namespace: ai
  labels:
    app.kubernetes.io/name: mcp-weather
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-weather
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
