apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai

resources:
  - deployment.yaml
  - service.yaml
  # - ingressroute.yaml   # uncomment to expose externally via Traefik

commonLabels:
  app.kubernetes.io/part-of: ai-stack
