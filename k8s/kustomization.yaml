apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai

resources:
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml
  # - ingressroute.yaml   # uncomment to expose externally via Traefik
  # - secret.yaml         # copy from secret.example.yaml, fill in token, uncomment

commonLabels:
  app.kubernetes.io/part-of: ai-stack
